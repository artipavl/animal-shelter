import React from 'react';

import css from './buttonHelp.module.css';

const ButtonHelp = () => {
  return (
    <button className={css.ButtonHelp} type="button">
      Допомогти
    </button>
  );
};

export default ButtonHelp;
